# tmux config for the dropterm dropdown. hides bar so it looks like a
# plain terminal
set -g status off
set -g mouse on
set -g default-terminal "tmux-256color"
# keep the session running when no client is attached, so it survives across
# toggles and niri restarts
set -g destroy-unattached off
